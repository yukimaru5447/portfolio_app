import { createTheme, Theme } from '@mui/material/styles'

const theme: Theme = createTheme({
  palette: {
    primary: {
      main: '#80cbc4',
    },
    secondary: {
      main: '#26c6da',
    },
    grey: { 200: '#eeeeee' },
  },
})

export default theme
